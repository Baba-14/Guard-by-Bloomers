import { Header,Footer } from '@/components/Header'; import Link from 'next/link'; import { ArrowLeft } from 'lucide-react';
export default function Article({params}:{params:{slug:string}}){const title=params.slug.split('-').map(x=>x[0]?.toUpperCase()+x.slice(1)).join(' ');return <><Header/><main className="page-shell"><div className="container"><Link href="/learn" className="small-link"><ArrowLeft size={14} style={{verticalAlign:'-2px'}}/> Back to Learn</Link><div className="page-title" style={{marginTop:26}}><div className="eyebrow">Fraud awareness guide</div><h1>{title}</h1><p>Fraud works best when we feel rushed. This guide helps you slow the moment down and spot the signals.</p></div><div className="form-panel"><h3>What it is</h3><p style={{color:'var(--muted)',lineHeight:1.65}}>A suspicious interaction often combines a believable story with a request for money, access, secrecy or sensitive information.</p><h3 style={{marginTop:28}}>Warning signs</h3><ul className="signal-list"><li>Urgency, fear or pressure to act immediately</li><li>Requests for OTPs, PINs, passwords or remote access</li><li>Payment recipient or website details do not match the story</li><li>Promises that sound guaranteed or unusually generous</li></ul><h3 style={{marginTop:28}}>What to do</h3><p style={{color:'var(--muted)',lineHeight:1.65}}>Pause. Do not click or pay. Verify through an official channel you find yourself, then use Guard to check the message, link, number or payment request.</p><Link href="/detect" className="btn btn-primary" style={{marginTop:18}}>Check something with Guard</Link></div></div></main><Footer/></>}
