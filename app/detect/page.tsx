import Link from 'next/link';
import { ArrowRight, BellRing, FileImage, Link2, MessageSquare, Phone, WalletCards } from 'lucide-react';
import { Header, Footer } from '@/components/Header';
const items = [['Check Message','Paste an SMS, WhatsApp message or offer.',MessageSquare,'/detect/message'],['Check Screenshot','Upload a suspicious conversation or payment request.',FileImage,'/detect/screenshot'],['Check Link','Inspect a URL before you open it.',Link2,'/detect/link'],['Check Phone Number','See moderated reports and reputation signals.',Phone,'/detect/number'],['Check Payment Request','Check recipient, amount and claimed identity.',WalletCards,'/detect/payment'],['Check Suspicious Call','Describe what the caller claimed and asked for.',BellRing,'/detect/call']] as const;
export default function Detect(){return <><Header/><main className="page-shell"><div className="container"><div className="page-title"><div className="eyebrow">Guard it first</div><h1>What would you like Guard to check?</h1><p>Start with a link or phone number, or choose the format that feels closest to what you received.</p><div className="detect-report-cta"><div><strong>Have you already been targeted?</strong><span>Report the number, link, payment account or message to help protect others.</span></div><Link href="/report" className="btn btn-outline">Report Fraud <ArrowRight size={15}/></Link></div></div><div className="detect-grid">{items.map(([t,d,I,href])=><div className="detect-card" key={t}><div className="icon-box"><I size={20}/></div><h3>{t}</h3><p style={{color:'var(--muted)',fontSize:14,lineHeight:1.5}}>{d}</p><div className="card-bottom"><Link href={href} className="small-link">Start check <ArrowRight size={14} style={{verticalAlign:'-2px'}}/></Link><span style={{fontSize:11,color:'#98a9b1'}}>Guest friendly</span></div></div>)}</div></div></main><Footer/></>}
